#optional parameter
# -->so that we don't have to use it again and again
# def func(x=1):
# 	return x**2

# call = func()#-->if we give any argument in it it  will overwrite it 
# print(call)
# ---------------------------------
# def func(word, add=5,freq=1):
# 	print(word*(freq+add) )
# call = func("hello",0)#-->order is important to change freq we have to give some argument for add
#----------------------------------